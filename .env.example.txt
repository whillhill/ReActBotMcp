# OpenAI兼容API配置
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo
# OPENAI_BASE_URL=https://api.openai.com/v1  # 如使用自建/代理服务可设置，否则可省略

# 心跳与超时配置（可选）
HEARTBEAT_INTERVAL_SECONDS=60         # 心跳检测间隔，单位秒，默认60
HEARTBEAT_TIMEOUT_SECONDS=180         # 心跳超时，单位秒，默认180
HTTP_TIMEOUT_SECONDS=10               # HTTP请求超时，单位秒，默认10
RECONNECTION_INTERVAL_SECONDS=60      # 重连间隔，单位秒，默认60

# ReAct智能体相关配置（可选）
REACT_MAX_ITERATIONS=5                # ReAct最大推理轮数，默认5
REACT_ENABLE_TRACE=false              # 是否启用ReAct执行轨迹，默认false

# Streamable HTTP端点（可选）
STREAMABLE_HTTP_ENDPOINT=/mcp         # 默认/mcp 
